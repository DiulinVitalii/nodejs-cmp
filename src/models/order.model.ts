import { ORDER_STATUS } from '../entities/Order.ts';

export interface OrderModel {
  payment: {
    type: string,
    address?: any,
    creditCard?: any,
  },
  delivery: {
    type: string,
    address: any,
  },
  comments: string,
  status: ORDER_STATUS;
}
